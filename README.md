# Pokemon Shiny Detector
Analyze a still frame of a Pokemon game, and identify if the encountered pokemon is shiny

# Dependencies
* pip install opencv-python
* OR: pip install opencv-python-headless
* pip install numpy
* pip install matplotlib
